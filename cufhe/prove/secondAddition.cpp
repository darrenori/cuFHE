#include <iostream>
using namespace std;

#include <string>

void half_adder(int a, int b){
	int sum;
	int carry;
	sum = a ^ b;
	carry = a & b;
	struct output {
		carry = a & b;
	       	sum};
	return output;
}

void full_adder(int carry_in, int a, int b){
	int Sum, C_Out;
	Sum = C_In ^ (A ^ B);
	C_Out = (A & B) || (B & C_In) || (A & C_In);
        return carry,sum;

